import { Header } from '../components/main/Header';
import { PortfolioList } from '../components/main/PortfolioList';

export const Home = () => {
    return (
        <>
            <Header />
            <PortfolioList />
        </>
    );
};

export default Home;
